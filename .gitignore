# all files ending with ~ at the end
*~

# ignore all the log files 
# start patterns with a forward slash (/) to avoid recursivity
/*.log

# end patterns with a forward slash (/) to specify a directory
logs/
